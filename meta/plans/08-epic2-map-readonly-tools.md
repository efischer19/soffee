# feat: Map read-only functions as OpenClaw tools

## What do you want to build?

Expose the newly created Python data-fetching functions to the agent by
defining them as tools within the `SKILL.md` manifest.

## Acceptance Criteria

- [ ] Add tool definitions for `get_current_matchups` and `get_team_roster` in `SKILL.md`.
- [ ] Provide clear descriptions for each tool so the LLM understands when to invoke them based on user questions.
- [ ] Define expected input parameters and types for the LLM.
